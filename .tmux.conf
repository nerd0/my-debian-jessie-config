set-window-option -g utf8 on
# Set a Ctrl-b shortcut for reloading your tmux config
bind r source-file ~/.tmux.conf
set -g default-terminal "screen-256color"

source /usr/local/lib/python2.7/dist-packages/powerline/bindings/tmux/powerline.conf
# Rename your terminals
#:set -g set-titles on
#set -g set-titles-string '#(whoami)::#h::#(curl ipecho.net/plain;echo)'
#
#set-option -g status on
#set-option -g status-interval 2
#set-option -g status-left "#(~/src/tmux-powerline/powerline.sh left)"
#set-option -g status-right "#(~/src/tmux-powerline/powerline.sh right)"
#
## Status bar customization
#set -g status-utf8 on
#set -g status-bg black
#set -g status-fg white
#set -g status-interval 2
#set -g status-left-length 90
#set -g status-right-length 60
##set -g status-left "#[fg=Green]#(whoami)#[fg=white]::#[fg=Cyan]#H#[fg=white]::##[fg=yellow]#(curl ipecho.net/plain;echo)"
#
#
#set -g status-justify left
##set -g status-right '#[fg=Cyan]#S #[fg=white]%a %d %b %R' 
#
## Notify when a window has activity
## # This quick snippet will have tmux notify you in the status area when a
## # window has activity:
#setw  -g monitor-activity on
#set   -g visual-activity  off # Display message telling that an activity
## happened (on/off)
#
